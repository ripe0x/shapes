# Decisions

Append-only. Two sections: decided (never silently revived if rejected) and open backlog (grouped by what resolves them). Ids are stable.

## Decided (recovered from specs and session history)

- 2026-09-03: D-44 RESOLVED by explicit user direction, closing audit finding V9-2's deploy-time half. Per-recipient fee accrual (D-43) stays: a recipient that cannot receive ETH still strands only its own balance, never the reserve or another recipient's. What changes is how the deploy path screens the initial recipient. It no longer refuses any recipient with code (`FEE_RECIPIENT_MUST_BE_EOA` in `script/deploy.sh`, `SHAPES_ALLOW_CONTRACT_FEE_RECIPIENT` in `script/Deploy.s.sol`, both removed); it instead proves the recipient accepts a plain ETH transfer before deploying. `Deploy.s.sol` deals itself 1 wei and calls the recipient with empty calldata in the simulation (never broadcast); `deploy.sh` runs the equivalent `cast call ... --value 1` before broadcasting, using `DEPLOYER` from the env file (anvil's well-known account 0 address when unset). A recipient that rejects the transfer refuses the deploy; the underlying rule is behavioral acceptance of ETH, not EOA-ness, so a contract wallet that forwards or stores plain ETH passes the same guard an EOA does. The mainnet recipient is the 0xSplits wallet `0xD4ba7cA95f3983514DDa317C4428CDb8F59c7e72` (`script/env/mainnet.env`), which stores callvalue and emits `ReceiveETH` on empty calldata. `test/Fork.t.sol` forks mainnet, deploys against this recipient, mints, withdraws, and asserts the recipient's balance rose by exactly the fee and a second withdrawal reverts `NoFeesPending`. `DEPLOYER` and `MINT_FEE_WEI` remain empty in `script/env/mainnet.env`; D-05 is still open and still blocks a real mainnet deploy.
- 2026-09-03: D-43 RESOLVED, closing audit finding S-1: `setFeeRecipient` could redirect fees already accrued to the previous recipient, which `IAdminControl` documented as impossible twice over (the admin "cannot reach ... accrued fees"; `setFeeRecipient` "already-accrued fees ... are unaffected"). Fee accrual is now per recipient rather than one shared counter. `Shapes` replaces the public `pendingFees` state variable with a private `mapping(address => uint256) _feesOwed` and a running `_totalFeesOwed`; `_mintBatch` credits the batch fee to `_feesOwed[feeRecipient]` (the recipient configured at that call) and to the total. `pendingFees()` keeps its selector and returns `_totalFeesOwed`, so the reserve invariant `balance >= redeemableBacking() + pendingFees()` is unchanged; a new `feesOwedTo(address) returns (uint256)` exposes one recipient's own share. `withdrawFees()` becomes `withdrawFees(address recipient)`: pays only `_feesOwed[recipient]`, zeroes only that entry, keeps CEI order and `nonReentrant`, and stays permissionless. `AdminOps.setFeeRecipient` is unchanged in body (it only ever wrote the pointer) but now correctly matches its own documentation: it moves no balance, so fees already credited to the outgoing recipient stay owed to it, withdrawable by anyone, independent of a later reverting or redirected recipient. `IShapes` moves from `0x9bde7d5a` to `0x9529d8c4`; `IAdminControl` is unchanged at `0x0ce8a022`. Runtime sizes default/testnet: Shapes 20,528/20,511 (was 20,342/20,325 per D-42). `test/audit/FeeAccounting.t.sol` and `test/Invariants.t.sol`'s stateful handlers were extended to drive `setFeeRecipient` against two known recipients and assert `sum(feesOwedTo(r)) == pendingFees()`. Also fixed in the same pass, unrelated to fees: three audit tests in `test/audit/PresentationLockOrdering.t.sol` and `test/audit/LibraryDirectCall.t.sol` pinned pre-887497c behavior (a collection unbound to this token installing successfully; `lockPresentation` succeeding with no collection set) and are rewritten to assert the rule 887497c actually enforces. Default profile passes 599 tests with 4 expected fork skips (was 596 passed / 3 failed against the merge base before these three were fixed).
- 2026-09-03: D-42 RESOLVED by explicit user direction. The apex Black-Shape operation `sacrifice(uint256)` is renamed `burnBacking(uint256)`, and its event `Blackened(uint256 indexed tokenId, uint256 sacrificedWei)` is renamed `BlackShapeCreated(uint256 indexed tokenId, uint256 burnedWei)`. Rationale: "sacrifice" described a ceremony; `burnBacking` states the fact that the ETH is burned to the unspendable address while the token survives and turns Black, and names what dies (the backing) rather than colliding in sense with the ERC-8060 `burn(uint256)` redeem entrypoint. `BlackShapeCreated` names the outcome and aligns with `blackShapeCount`. `burnedBacking` was already named correctly and is unchanged. `IShapes` moves from `0x2ba22587` to `0x9bde7d5a`; the event topic0 is `0xa1192b540aa1e4ace8500c2dbe727984ed3e63552f99909f7ac7d247b2e445aa`. Shapes runtime size is unchanged at 20,342 bytes default profile. Default profile passes 530 tests with 4 expected fork skips.
- 2026-09-03: D-41 RESOLVED by explicit user direction. The metadata copy lives on the collection; the token keeps no presentation storage. `ShapeCollection` stores the token name prefix and the shared description, validates both with `CopyValidation` (64-byte prefix, 2048-byte description, JSON-safe well-formed UTF-8) and holds `setMetadataCopy`, gated on `admin()` and `presentationLocked()` read live from the `Shapes` it is constructed with, so there is no second admin role and no second lock. `Shapes` loses `_copyConfig`, `tokenNamePrefix()`, `description()`, `setMetadataCopy` and `AdminOps.CopyConfig`; `tokenURI` and `contractURI` read the copy back from `collection()`. `CopyValidation` now links into `ShapeCollection` instead of `AdminOps`. Because the collection takes the token's address at construction, `Shapes`'s constructor drops its `collection` parameter and deployment fills the pointer with the existing admin `setCollection` before anything else runs; `tokenURI` and `contractURI` revert `CollectionNotSet()` while it is zero. The collection cannot emit ERC-4906 from the token's address, so a copy edit is two transactions: `collection.setMetadataCopy`, then `Shapes.refreshMetadata()`, the one new admin entrypoint, which emits `BatchMetadataUpdate(0, totalMinted - 1)` and `ContractURIUpdated()`. `setCollection` emits the same two signals, since a new collection carries new copy for every token. `IShapes` is `0x2ba22587` (was `0xaaf9b098`); `IAdminControl` is unchanged at `0x0ce8a022`. Runtime sizes default/testnet: Shapes 20,342/20,325 (was 20,377/20,360), AdminOps 2,848/2,847 (was 3,755/3,754), ShapeCollection 5,882/5,875 (was 4,077/4,070), ShapeRenderer 23,442/23,441 unchanged. Default profile passes 530 tests with 4 expected fork skips.
- 2026-09-03: D-40 RESOLVED by explicit user direction. The token owns every protocol fact. `Shapes` now carries every action, every view and both previews, reachable from the token address alone: `exists`, `positionOf`, `shapeState`, `composeRecordAt`, `splitOriginOf`, `isSupportedDenomination`, `unicodeCard`, `previewCompose(account, ...)` and `previewSplit(account, ...)`. `ShapeLens.sol`, `IShapeLens.sol`, `DeployLens.s.sol` and `LensEquivalence.s.sol` are deleted, which closes R5 by construction: a preview can no longer be deployed against different library code than the token executes. The room came from structure, not squeezing: the compose/decompose/split state machine moved into the linked `RecompositionOps` library over one `ShapeStore` storage pointer, and every configuration write into `AdminOps`, which absorbs `PointerOps`. Neither library writes ERC-721 state, moves ETH, or touches the owner token or the admin address; every access check runs in `Shapes` before it delegates. Storage types moved to `src/ShapeTypes.sol`; the three ERC-165 capability interfaces moved onto `IShapes.sol` and `IShapeCapabilities.sol` is deleted. ABI removals: `composeRecordHeaderAt`, `composeRecordInputAt` (panicking bounds and an id-plus-one encoding leak), `gridForAmount` and `modulesForAmount` (the renderer's `IShapeGeometry.cardGeometry` already answers). Renames: `lockRenderer`/`rendererLocked` to `lockPresentation`/`presentationLocked` because the one lock freezes the collection too, `RendererLocked()` to `PresentationLocked(renderer, collection)`, `RendererIsLocked` to `PresentationIsLocked`, `EmptyRecomposition` to `NoComposeInputs`, `SplitMismatch` to `SplitSumMismatch`, `splitOriginRaw` to `splitOriginOf`. `blackShapeCount` becomes the count of Black Shapes alive now, so burning one for zero lowers it; `burnedBacking` stays cumulative. `setPointer` requires a nonzero target to answer ERC-165 for the interface its reader calls, and `Deploy.s.sol` registers the auction house as `market`. R2 is closed: `previewCompose`/`previewSplit` take the account whose ownership is assumed and share `requireLiveOwner`, `requireComposeInput` and `requireDistinct` with the mutators, so a repeated burn id is rejected on both sides with `DuplicateComposeInput` rather than one side reporting `ERC721NonexistentToken`. Measured, default/testnet: Shapes 20,370/20,353 bytes (4,206/4,223 of EIP-170 margin, from 23,795 before with less surface), RecompositionOps 12,246/12,228, AdminOps 3,747/3,746, ShapeRenderer 23,442/23,441, ShapeAuctionHouse 8,015/8,006, ShapeCollection 4,077/4,070. `IShapes` is `0xaaf9b098`. Correction to D-37, which recorded `0x86df37ba`: the following comment and naming pass moved it to `0x374d44fd` before this change. Both profiles pass 519 tests with 4 expected fork skips. The design is written up in `project/ARCHITECTURE.md`, which is the permanent reference. Not deployed: the recorded Sepolia deployment predates this ABI and must be redeployed before the site points at it.
- 2026-09-02: D-39 RESOLVED by explicit user direction. Mainnet public mint opens 2026-09-03 12:00 ET (unix `1788796800`), enforced by an immutable constructor value: `mintStart` is a `uint64` set once and read-only thereafter, and `_mintBatch` reverts `MintNotOpen()` on `mint`, `mintTo`, `mintBatch`, `mintBatchTo` and the auction bids that mint through `mintBatchTo` while `block.timestamp < mintStart`. The constructor mint of Shape #0 is unconditional and unaffected. There is no deployer exemption and no other pre-launch mint; the user does not mint #1 themselves before the public start. Rationale: no admin authority ever exists over minting, the value is readable onchain rather than trusted, and the same `script/Deploy.s.sol`/`script/deploy.sh` path rehearses it on Sepolia before mainnet. `script/env/mainnet.env` sets `MINT_START=1788796800`; `script/env/sepolia.env` leaves it empty for a rehearsal override.
- 2026-09-02: D-38 RESOLVED by explicit user direction. One deploy script and one wrapper cover every environment: `script/Deploy.s.sol` deploys anvil, Sepolia and mainnet alike, gated by chain id (mainnet requires the mainnet ladder, Sepolia the testnet ladder, anvil accepts either); `script/deploy.sh <anvil|sepolia|mainnet>` runs it identically for every target, with environment differences carried only as data in `script/env/<name>.env`. Rationale: the fork test (`test/Fork.t.sol`) and the Anvil deploy/e2e path must exercise the exact deploy that reaches Sepolia and mainnet, not a parallel local-only script that can drift from what actually ships. Consequence: `script/DeployShapes.s.sol`, `script/DeploySepolia.s.sol` and `script/deploy-sepolia.sh` are removed; the next Sepolia deployment uses the unified path (`script/deploy.sh sepolia`); mainnet remains blocked on D-05 until `script/env/mainnet.env`'s `DEPLOYER`, `FEE_RECIPIENT` and `MINT_FEE_WEI` are filled in.
- 2026-09-02: D-37 RESOLVED by explicit user direction, GitHub issue #56, superseding the #0-only reading of D-24/D-35. Collection ownership follows one live Shape, the owner token, rather than being pinned to id 0. It starts as #0; a compose that absorbs it moves it to the survivor (recorded in the compose record's `ownerTokenFrom`); the matching decompose restores it to that input; a split of it gives it to the first output, deterministic because the caller already orders `outDenoms`; `decomposeTo`/`splitTo` make the chosen recipient the collection owner. Transfer and auction follow the holder with no special path. Redeeming or `burn()`ing the owner token ends collection ownership permanently: `owner()` returns zero and no token inherits, with no renounce entrypoint and no rejection path. `owner()` keeps its selector and meaning; `ownerToken()` is added, reverting `NoOwnerToken()` once none remains; `event OwnerTokenMoved(uint256 indexed fromTokenId, uint256 indexed toTokenId)` fires at genesis, compose, decompose, split and redeem/burn, with `type(uint256).max` as the none id. Holding the owner token grants no administrative authority, unchanged; no authorization check reads `owner()` or `ownerToken()`. The byte-recovery step this issue required first extracted `attestArtist`, `setMetadataCopy`, `setFeeRecipient` and `setMintFee` into one `AdminOps` library called via delegatecall (recovering 803 bytes: Shapes 24,397 to 23,594 bytes default, 24,376 to 23,574 testnet) and lowered `optimizer_runs` to 20 (a further 114 bytes of margin), bringing the deployed source count from eleven to twelve. The requested 1.5 KB free margin was not reached within the allowed scope; the accepted margin instead is measured post-feature at 970 bytes default and 990 bytes testnet. Final measurements: Shapes 23,606/23,586 bytes (970/990 margin), ShapeRenderer 23,437/23,436, ShapeLens 10,826/10,808, ShapeAuctionHouse unchanged at 7,939/7,930. `IShapes` is `0x86df37ba`. Default and testnet profiles pass 508 tests each with 4 expected fork skips.
- 2026-09-02: D-35/D-36 SEPOLIA EVIDENCE COMPLETE. Exact clean `origin/main` commit `eb9e8834553f199a4c94e7ba307686c8bd0d64e8`, carrying code-bearing audit target `1054db2455f7d6d3542a422130262bc872c34464`, deployed from block 11616988. Shapes `0xb142c4b09c24d639d8c154c93a539cbc09566152` reads the exact 10000000000000-wei testnet flat fee and points to renderer `0xd9c3278d1277cef31b54e98a43db4243ada05610`; collection `0x8c5203d5cd480f7e0b266a2f6d27f0ed9919e8e1`, lens `0x259e90f875b7b975c09f05e5972f359ee0a3fa84` and auction house `0x351b7c9637c6abc1982be95f87961aff2f38647a` are wired exactly. Shapes creation transaction `0xeb47218282fe64db1124b8369c5f54056fb23391e14b1dd2decfb8079a4cfdec` and all other creation receipts succeeded; all eleven contracts/libraries are Etherscan-verified. Reserve, roles, Shape #0 backing/ownership, empty unlocked pointers and zero-auction postflights passed. Fly schema `shapes_sepolia_v3` and the production hybrid site are synchronized to this deployment. External audit, D-15, R25 signoff and D-05 remain open; no mainnet deployment is authorized.
- 2026-07: Ladder rung 25 ETH replaced by 0.05 ETH; the x2.5 gap at 10 to 25 broke integer composition (SHAPES_V2_SPEC.md §8).
- 2026-07: Core "Token Path" registry cut for EIP-170 and write-only-no-reader reasons. Must not return as a shared global registry; lineage is per-work at the write authority, global graph only ever a derived read layer.
- 2026-07/08: `seal` mechanism rejected outright (ZERO_AUCTION_DRAFT.md F1). Not a deferral.
- 2026-08: No auction-house protocol fee, structurally excluded (card lattice cannot represent a percentage).
- 2026-08: Renderer and collection deliberately not locked at launch (ZERO_AUCTION_DRAFT.md R14).
- 2026-08: Epoch commit-reveal seeding considered and deliberately not built; grinding accepted because redemption value is seed-independent. Revisitable (D-19).
- 2026-08: Shapes split core/periphery (ShapeLens + externally linked libraries) to stay under EIP-170; lens/library addresses fixed in bytecode, not admin-settable. Superseded by D-40, which deletes ShapeLens and moves the bodies into delegatecall libraries over Shapes storage.
- 2026-08-20: Compose/split geometry sampled from input modules, materialized as bytes (SAMPLING_SPEC.md); originals stay seed-derived; GRAMMAR_VERSION 2.
- 2026-08-25: Canonical project docs live in `project/`; STATE.md supersedes status lines in legacy spec docs (this session).
- 2026-08-25: D-04 DECIDED by user: standalone Shapes is the mainnet architecture. The Surface-hosted alternative (pooled Surface clone + ShapesMinter extension owning economics, fork-tested against the live mainnet SurfaceFactory) is REJECTED — do not silently revive. Branch preserve/surface-port deleted deliberately with user approval; the design is documented here and in the port's history (commit ce4c7e4 recorded before deletion). The per-work lineage doctrine that came out of that work stands independently: lineage lives at the write authority, any global graph is a derived read layer, never an authoritative singleton.
- 2026-08-26: D-26 RESOLVED by explicit user instruction. Add permanent artist attribution and an actual cryptographic signature, with no artist statement and no two-step admin transfer. `artist()` is the Shapes deployer and is attribution only. A direct-core EIP-712 prototype grew Shapes from 24,131 to 25,611 runtime bytes, 1,035 over EIP-170, so it was rejected by measurement. The adopted design keeps `artist()` and an immutable child pointer in Shapes; the constructor creates `ShapesArtistAttribution`, whose one-time EIP-712 signature binds chain id, exact child, exact Shapes address, artist and release hash. Canonical ECDSA is tried even for a code-bearing address so an EIP-7702 delegated deployer EOA remains usable; ERC-1271 is the contract-wallet fallback. For Sepolia the `releaseHash` is the exact Shapes creation transaction hash, a stable onchain provenance anchor already complemented by the signed chain/Shapes/child/artist fields. The final core is 24,289 bytes with 287 bytes of margin. `IShapes` evolves normally to `0xca355cbe`; as with D-25, there is no predeployment compatibility issue.
- 2026-08-26: D-27 RESOLVED by explicit user instruction, Charter amendment 2. `feeBps` remains immutable, but `admin()` may update `feeRecipient` for future mints. The setter cannot recover fees already sent, withdraw ETH, alter backing or redemption, or affect token ownership. Renouncing admin freezes the final recipient. Sepolia launches with payout `0x41c3BD8A36f8fE9Bb77900ca02400b32BB35A6A4` and the deployer as admin. This changes `IAdminControl` to `0xe135adbe`, leaves `IShapes` at `0xca355cbe`, and leaves Shapes at 24,369 runtime bytes, 207 below EIP-170.
- 2026-08-26: D-28 RESOLVED by explicit user instruction, superseding D-26's child-contract implementation before launch. Artist attestation now lives directly in Shapes: `artistReleaseHash`, `artistSignature`, `artistAttestationDigest` and `attestArtist`. A stateless, reusable linked `EIP712Signature` library holds only digest and EOA/ERC-1271 verification code; it stores no attribution and has no authority. To make direct storage fit coherently, token and collection metadata now share one editable `description`, `setMetadataCopy` updates the token prefix and shared description atomically, and `contractURI` uses the immutable ERC-721 name `Shapes` rather than a contradictory mutable collection name. The separate `ShapeCollection` remains because merging its exact animated output into the 22,699-byte renderer would exceed EIP-170. Final default-profile sizes are Shapes 24,103 bytes (473-byte margin) and `EIP712Signature` 1,009 bytes. `IShapes` is now `0x926c1806`; no predeployment compatibility issue exists.
- 2026-08-26: D-29 RESOLVED by explicit user direction. The future positions protocol is an external, fully escrowed exchange-option layer. A creator pairs a live Shape with claim assets, snapshots its value and an expiry, and escrows only the claim; the Shape remains an ordinary transferable NFT. Its current holder may atomically transfer the Shape to the creator in exchange for the claim. A missing Shape or value mismatch invalidates exercise and permits creator recovery; expiry also permits recovery. A gacha may separately custody the Shape while it is offered as a prize. `positionOf` reports at most one canonical active position; Shapes performs discovery only. No freeze, wrapper, mutation nonce, claim custody or exercise logic is added to Shapes. Implementation stays parked until post-launch.
- 2026-08-26: D-30 RESOLVED by explicit user direction. Deploy releases only from `main`, never from a feature branch. PR #4 may merge before the D-28 Sepolia deployment because the site now treats missing `artist()` and `artistReleaseHash()` selectors as unavailable attribution rather than failing its entire state load. The current Sepolia deployment remains usable after merge; the exact merged `main` commit is then deployed and `web/public/deployment.json` is updated in a small follow-up cutover.
- 2026-08-26: D-31 RESOLVED by pre-deployment core-surface review, relating to D-18/D-29. ACCEPT `exists(uint256) -> bool`: it is a non-reverting view over ERC-721 liveness already owned by Shapes, true for every live token including Black and false for never-issued, redeemed/burned, split-parent and compose-consumed ids. ACCEPT live-only `denomIndexOf(uint256) -> uint8`: the index is already stored in `ShapeData`, must return 0..8 including 8 for Black, and must revert for a nonexistent id like the other token-state getters. Declare both on `IShapes`; also add `denomIndexOf` to the denomination-oriented `IShapeValue` capability, while leaving generic ERC-721 liveness out of that narrower interface. REJECT `absorbedBy(uint256)` as proposed: positions and gacha need no reverse compose lookup; the relationship already lives in compose records and filterable events; a reverse mapping would duplicate it at roughly one new storage slot and ~22,100 compose gas per burned input (plus a cold nonzero-to-zero clear on decompose, ~7,100 gas before refunds), and “holds its revivable identity” becomes false or requires more state/iteration after the survivor is redeemed, split, consumed into another survivor or made Black. Do not revive it without a concrete onchain consumer and precise immediate-parent/abandoned-record semantics. Resolver lifecycle hooks, per-token mutation nonces and batch/aggregate core views remain rejected: the seam stays powerless, observable state already exposes relevant mutation, and aggregation remains lens territory. Adding the two accepted selectors changes `IShapes` from `0x926c1806` to expected `0xb5ac96e9`; `IShapeValue` also evolves normally. This is ordinary pre-deployment interface evolution under D-25, with no dual-id compatibility. Their implementation is gated by default/testnet EIP-170 measurements against the current 473/494-byte margins; an overflow returns for explicit scope tradeoff rather than silently dropping or moving functionality.
- 2026-08-26: D-31 IMPLEMENTATION EVIDENCE. The two accepted getters and direct ShapeLens consumption fit without new storage: Shapes is 24,235 bytes in the default profile (341-byte EIP-170 margin) and 24,214 bytes in testnet (362-byte margin). `IShapes` is confirmed at `0xb5ac96e9`; `IShapeValue` is `0xd07d718a`. Both full profiles pass 443 tests with 4 fork-only skips, and the preview/web checks pass. The rejected `absorbedBy` mapping remains absent.
- 2026-08-27: D-07 RESOLVED FOR P1 TESTNET. The seed-preserving Monte Carlo factory now carries each actual survivor seed and XORs the actual burn seeds at every rung. At the committed constants, its retained-Dense heuristic reaches a Solid 100 after 40,064 dust mints on average (95% CI 39,972–40,156), about 4.01 ETH in mainnet-scale mint fees, with 100.07 ETH mean peak retained backing. This is a measured policy, not a global optimum. Keep the constants unchanged for Sepolia: 40k mints plus 100 ETH final backing is materially difficult, while the experiment does not justify inventing replacement odds. Reconfirm the immutable constants during P2 mainnet-value signoff after Sepolia observation; no silent constant change is authorized. Evidence: `project/experiments/EXP-002-ink-gene-distribution.md`.
- 2026-08-27: D-08 RESOLVED; W-2 REJECTED. A direct 10,000-input compose costs 1,133,751,468 gas, and direct 10,000-way split/decompose cannot fit a 30M-gas call. The exact ladder tree reaches the same 100 ETH Complete through 3,333 bounded compose calls; the worst individual rung measured 985,862 gas, and a measured 10-call `composeMany` fits at 9,773,061 gas. The P1 stop condition is not triggered because the core hierarchical path is feasible, but no UI may offer a one-shot 10k fan-in/fan-out and no unmeasured production batch size may be asserted. The existing local apex simulation adds no L1 evidence, so W-2 is deliberately not ported. Evidence: `project/experiments/EXP-001-gas-ceilings.md`.
- 2026-08-27: D-09 RESOLVED. Adopt Medusa 1.5.1 as a bounded stateful complement to Foundry, not as a formal proof. Its reserve/lifecycle campaign passed 10/10 properties and 34,350 calls in the Director rerun; CI pins the binary and verifies its SHA-256 before execution. The oversized Medusa-only harness lives outside Foundry's normal build tree so the production EIP-170 gate remains meaningful. Halmos 0.3.3 is deferred after its AST build failed to reach symbolic execution inside the three-minute/~2.1 GB spike budget. Evidence: `project/experiments/EXP-003-formal-tool-spike.md`.
- 2026-08-27: D-12 RESOLVED. Browser reads and the server OG route share the same Sepolia fallback policy: optional configured primary, deployment RPC, then three independent public providers. Deterministic blackhole tests prove both clients advance past a dead primary. R6 is closed.
- 2026-08-31: D-13 RESOLVED FOR THE P1 AUCTION GATE. Live Sepolia auction #0 at `0x603C745cBFCC76ad47E1eCf6b875abC995959801` sold Shape #1 from `0xCB43078C32423F5348Cab5885911C3B5faE217F9` to `0xea194A186EBe76A84E2B2027f5f23F81939c05AD`. Two ETH-backed bids minted and escrowed one testnet dust Shape each: `0x8dfbd9f31ac94450b52797aeeae00f374cec1b5f6cfb34a398ee7d4464374c07` recorded 1 unit with end time 1787980404, then `0x6c0a433e3c4a0d00971a52d797d20a1399a7f43399e34455f4618fbc5efda657` accumulated 2 units and extended the end to 1787980416. The 5% minimum increment therefore behaved as designed on the integer card lattice: from a 1-unit lead, the next valid total was 2 units. Settlement `0x2855bd12ea9b27fb323cf1d69680f33a6de1aa49274806244211e0cacb19a893`, lot claim `0xc45fcca9e901eb3e7204b4095afad78520a0f3a514f38b93dcc7c1cbead5ea84`, and proceeds claim `0xe6749f28f05486193c43a2ab8cfe4eb522d0111a2e5df34a83ecc76ea7c6f4f0` all succeeded. Final reads show `settled=true`, `lotClaimed=true`, Shape #1 owned by the winner, Shapes #2 and #3 owned by the seller, winner `bidUnits=0`, no active lot index, and zero ETH and zero Shapes retained by the house. The Fly indexer reached the same Sepolia head block 11608016 and reported those three owners exactly. The only operational failure was depleted bidder gas during the first close attempt; PR #34 made the close script resumable and the retry completed without changing protocol semantics. One controlled rehearsal validates lifecycle and UX mechanics, not market-wide parameter preferences, so no protocol default change is inferred.
- 2026-08-27: P1 SITE/OPERATIONS PACKET RESOLVED. W-1 now keeps Black Shapes visible with canonical artwork while hiding invalid mutators and renders provenance rollups as `+N more`, never fake token #0. W-4 adds CI cancellation, path filters, Foundry caches and an explicit web/site job. W-5's React bigint dev crash guard is present. W-6 adopts portless routing. D-21 removes the unused direct `@x402/*` dependencies. Next is upgraded to 16.3.3, and the Sepolia deploy wrapper now enforces exact clean `main`, approved sender/payout/fee/ladder values, complete wiring readback and Etherscan source verification.
- 2026-08-28: D-10 RESOLVED. The optional Ponder path supplies candidate live ids only; exact count/uniqueness, freshness within two blocks, chain identity and every displayed/actionable token field are verified against current Shapes state, with deterministic raw-RPC fallback on any failure. The 1,203-id fixture reduces state reads from 1,218 to 18 (98.52%). PR #20 (`f92d019`) deploys Ponder 0.17.8 as one Fly machine with embedded PGlite on an encrypted 1 GB volume, the same simple operating pattern proven by the user's recent projects and with no separate database service. Live activation passed health, readiness, status and GraphQL checks; Shape #0 indexed with exact testnet backing and the checkpoint was one block behind Sepolia. Tenderly's free endpoint produced a handled 429 during backfill and PublicNode fallback completed it; no paid/archive RPC is required for the current event range.
- 2026-08-27: P1 FRONTEND DEPENDENCY HARDENING RESOLVED. Pin Vite 6.4.3 and override transitive Axios to 1.20.0 plus only WalletConnect's nested Viem `ws` to 8.21.0. This removes every high/critical root npm advisory without a Wagmi major migration: the audit falls from 26 findings (3 high) to 9 moderate and 0 high/critical. Preview's 47 tests/build and the Next lint/build pass on the exact lockfile. The residual moderate advisories are in unused Gemini/MetaMask connector dependencies beneath Wagmi; Shapes configures injected wallets and conditionally WalletConnect only, so they are installed but not selected application paths. Reassess the remaining Wagmi 2 dependency graph during D-11's real WalletConnect/mobile validation rather than forcing an unrelated pre-deployment API migration.
- 2026-08-27: P1 SECURITY/RELEASE HARDENING RESOLVED. Codex Security scan `af61993b-3d85-4142-984b-19343d4697ae` reviewed exact pre-fix range `7f92f1b..5d1c37e` and found no core-contract issue plus three low site/indexer issues: a stalled indexer could block fallback, hostile pagination/body data was not locally bounded, and RPC-forged token metadata could give the server OG renderer an external image URL. The final packet closes them with an 8-second per-page abort, 256 KiB response cap, 500-item pages, totalSupply-derived item/page ceilings, unique cursors and bounded passive embedded-SVG validation; focused regressions prove fallback and legitimate artwork. Release correctness fixes also fetch `origin/main` before the deployment equality check, omit operational RPC credentials from portable summaries, exercise both ladder fixtures/parity in CI, isolate Medusa extraction from the repository's `medusa/` directory, trigger renderer CI on the root lockfile and add an isolated indexer CI job. These are boundary hardening only; Shapes runtime and public ABI are unchanged.
- 2026-08-27: D-32 RESOLVED by Director review of GitHub issue #7. Rescope the behavior-preserving `ShapeRenderer._moduleSvg` extraction as P2 pre-mainnet audit hardening, after the current P1/Sepolia release and before any renderer module, vocabulary or grammar expansion; it is not a P1 deployment blocker. Scope is private pure per-kind helpers with unchanged dispatch, public ABI, canonical TypeScript renderer, fixtures, frozen legacy oracle, metadata, geometry and serialized output. `_glyph` remains unchanged and is a documented complexity exception: its score is produced by a shallow, cohesive pure lookup table, where direct mapping is safer to audit than abstraction. Acceptance requires full default/testnet suites; byte-identical parity against unchanged TypeScript fixtures; frozen-legacy differential, exhaustive denomination/gene/inversion and valid-module-byte coverage; before/after runtime sizes targeting no growth over the measured ShapeRenderer baselines of 22,699 bytes default and 22,698 bytes testnet; and pinned before/after gas for seed-derived and sampled SVG/metadata/tokenURI at one- and 25-module extremes. Any runtime growth or worst-case gas regression above 5% returns for a separate Director decision. PR #36 merged the accepted implementation as `1d6e4b0`, with full evidence in EXP-004. Owner: Project Director implementation/review; `ripe0x` final merge approval.
- 2026-08-31: D-32 BASELINE REFRESH. The merged issue #21 sampling/provenance work intentionally changed renderer behavior before the D-32 refactor began. Fresh exact-main measurements are therefore the applicable pre-refactor baselines: ShapeRenderer 23,185 bytes default and 23,184 bytes testnet; Shapes 24,394 bytes default and 24,373 bytes testnet. D-32's behavior-preserving scope, no-growth target and 5% gas review threshold are unchanged.
- 2026-08-31: D-32 IMPLEMENTATION COMPLETE. Issue #7's private per-kind helper extraction preserves the unchanged TypeScript fixtures and frozen Solidity oracle byte for byte. Normalized complexity falls from 16 to 10 for `_moduleSvg`, and every helper is 3 or lower. ShapeRenderer runtime falls by 47 bytes to 23,138 default / 23,137 testnet. The worst pinned gas regression is 0.0104%, both 453-test Foundry profiles pass with 4 expected fork skips, and all 115 preview tests plus fixture regeneration, stream verification and collision sweep pass. Evidence: `project/experiments/EXP-004-renderer-module-refactor.md`. This closes the D-32 implementation gate without authorizing any renderer vocabulary or grammar expansion.
- 2026-08-28: D-33 RESOLVED by Director review of GitHub issue #6. Defer `ShapeGrowthEscrow` to P4 and build it only for a concrete consumer. The escrow pattern is compatible with immutable core and D-29's external-custody direction, but it is not a launch dependency and would add a separately auditable NFT custodian. Eventual baseline scope is deposit/withdraw, depositor-only composer rotation/revocation and composer-only `composeInto`, with no `Shapes.sol` or `IShapes` change. Exclude `decomposeInto` until contributor economics are explicitly decided, and exclude a generic `positionOf` adapter because custody alone is not a canonical D-29 exchange-option position.
- 2026-08-31: D-34 RESOLVED by explicit user direction, Charter amendment 3. Replace the specialized `positionResolver` surface with two explicit canonical pointers: `positions()` and `market()`, each returning its target and independent permanent-lock state. Both launch empty and unlocked. Admin uses the size-conscious `setPointer(uint8,address)` / `lockPointer(uint8)` pair, where 0 is Positions and 1 is Market; nonzero targets must contain code, either entry may be changed or cleared before locking, and either may be locked forever at zero without affecting the other. Admin renunciation makes every unlocked entry practically permanent at its last value. Canonical means surfaced by Shapes, not exclusive. Core state-changing operations never read or call either target. `ShapeLens.positionOf` remains the optional convenience read, resolving only through the positions target with a 50,000-gas cap and failure/malformed-return conversion to zero; the target may aggregate later position systems. The market is pure discovery and is never called. `PointerOps` is a stateless linked library used only through admin-gated Shapes wrappers; it mutates the two Shapes storage slots by delegatecall and has no authority at its own address. The new `IShapes` id is `0xbdf217ea`. Against exact `6cb10ed`, runtime moves from 24,394 to 24,474 bytes default (182 to 102 bytes EIP-170 margin) and from 24,373 to 24,453 testnet (203 to 123 bytes); ShapeLens moves from 9,661 to 9,885 default and 9,643 to 9,867 testnet; PointerOps is 605 bytes. This margin is accepted only with the existing strict dual-profile size gate and no further core growth without explicit recovery. PR #39 merged the design as `8d8c2b2`; release-fork fixes merged in PR #40. Exact deployed release source is `dba4dbfe93df64cc72052c3eab70289070e301d9`, and `AUDIT_PROMPT_v5.md` supersedes snapshot `020a85e` for release audit purposes.
- 2026-09-01: D-35 RESOLVED by explicit user direction. Token #0's fixed metadata name is `Shapes Collection Owner`, independent of the editable ordinary-token prefix, and it alone carries the string-valued trait `Collection Owner: true`. This describes the already-adopted transferable collection role and creates no authority; `admin()` remains separate. The site uses the same full identity on token, auction and management views, plus compact `Collection Owner` copy in token grids. The change affects only ShapeRenderer and presentation: Shapes remains 24,474/24,453 bytes, while ShapeRenderer moves from 23,138/23,137 to 23,331/23,330 bytes default/testnet, leaving 1,245/1,246 bytes of EIP-170 margin. Each Foundry profile has 461 passing tests plus 4 expected fork skips, byte parity includes #0, all 126 preview tests pass, and preview/web builds plus site lint pass. PR #46 merged the implementation with D-36 as `1054db2`; the current Sepolia renderer remains the earlier immutable release.
- 2026-09-01: D-36 RESOLVED by explicit user direction, superseding the percentage amount in D-27 while retaining D-27's fee-recipient authority boundary. Mainnet charges one immutable flat 0.001 ETH fee for every Shape created, regardless of denomination; the 1/100 testnet build charges 0.00001 ETH. Direct and batch mints pay one fee per output NFT. An auction ETH bid pays one fee per minimal card created, and `mintCostFor(backingWei)` exposes the exact quote; card bids mint nothing and pay no mint fee. This deliberately removes denomination-proportional path parity: direct 100 ETH artwork rerolls cost 0.001 ETH per candidate, while composing 10,000 dust mints pays 10 ETH. Solvency and redemption are unchanged because fees never enter backing. `IShapes` changes from `0xbdf217ea` to `0x86cf5406`. Against exact pre-change `3eed6d0`, Shapes runtime falls from 24,474/24,453 to 24,362/24,341 bytes default/testnet, leaving 214/235 bytes; ShapeLens remains 9,885/9,867; ShapeRenderer remains 23,331/23,330; ShapeAuctionHouse grows from 7,872/7,862 to 7,939/7,930. Default, testnet and deeper CI profiles pass 462 tests with 4 expected fork skips; all 4 fork tests pass against live Ethereum. PR #46 merged D-35/D-36 as `1054db2`; fresh external audit and Sepolia evidence remain required before mainnet.
- 2026-08-27: D-11 INTEGRATION EVIDENCE, MOBILE MINT PENDING. The project-owned public Reown id is configured in Netlify's production, deploy-preview and branch-deploy build contexts. A deployed PR preview exposes the WalletConnect connector and successfully opens a scannable relay QR. That live check found and fixed an upstream compatibility break: RainbowKit 2.2.11's `cuer` 0.0.3 requests a borderless QR while newly resolved `qr` 0.6.0 rejects `border: 0`; pin `cuer` to `qr` 0.5.5 and retain a direct regression test of the exact QR path. Preview now passes 56 tests, web lint/build pass with the configured id, and the root production audit remains 9 moderate / 0 high or critical. D-11 is not closed until a real phone pairs and completes a Sepolia mint.
- 2026-08-27: D-11 MOBILE WALLET INVENTORY CORRECTED. Real iPhone evidence showed only `Browser Wallet` and generic `WalletConnect` because Shapes manually registered exactly those two entries. Use RainbowKit 2.2.11's maintained default inventory instead: named Rainbow, Base Account, MetaMask and WalletConnect choices, with Safe shown only in its applicable context. Retain injected-only fallback when the project id is absent. The deterministic config regression requires at least five default connectors and the live mobile modal remains the release authority. Selecting the MetaMask connector means the nine moderate transitive advisories can no longer be described as wholly unselected paths; they remain 0 high/critical, require a Wagmi 3 migration for npm's supported fix, and stay an explicit dependency risk rather than being silently dismissed.
- 2026-08-27: D-11 RAINBOW NAMESPACE REMEDIATION IN VALIDATION. After the named Rainbow option appeared, real iOS pairing failed inside Rainbow with `No accounts found in approved namespaces`. The failure is consistent with the deployment's testnet being the WalletConnect session's only configured chain. Advertise Ethereum mainnet first as the broadly supported session bootstrap and Sepolia as an allowed application chain; identify chain 11155111 as Sepolia rather than `Shapes dev chain`. This does not redirect any Shapes operation: `usePublicClient` and native-balance reads are explicitly pinned to the deployment chain, and every Shapes/auction write switches to that chain before submission. The config regression pins `[1, deploymentChainId]`. This remains provisional until the same real Rainbow account connects and completes a Sepolia mint.
- 2026-08-27: D-11 MAINNET BOOTSTRAP REJECTED BY LIVE EVIDENCE. The provisional mainnet-bootstrap workaround allowed Rainbow pairing but authorized only mainnet; the Sepolia switch before mint produced no wallet prompt and surfaced as a rejected transaction. Remove that workaround immediately. Require the deployment chain directly in the underlying WalletConnect session proposal, keep Wagmi configured to that single chain, and rotate WalletConnect's storage prefix so the invalid mainnet-only session cannot be reused. For chain 11155111 the wallet must connect on canonical Sepolia and mint without an intermediate network dance. Explicit deployment-chain reads and balance checks remain as defense in depth. Live Rainbow connect plus Sepolia mint is still the authority.
- 2026-08-27: D-11 USE VIEM'S CANONICAL SEPOLIA CHAIN. Rainbow kept showing `Network: None` and connection failed. A probe of the real connector proved the config already sent Sepolia (`chains=[11155111]`), so the namespace was not the problem, and forcing a required namespace only broke the connect. The actual deviation from every other working RainbowKit app in this account (signature, based-opepen, funding-works, hunt, commonground, ...): they all pass the canonical chain object imported from `wagmi/chains`/`viem/chains` and apply custom RPC only through `transports`. Shapes instead hand-built the Sepolia chain with `defineChain(...)`. WalletConnect/Rainbow identify the network from the chain definition, and a hand-rolled one reads as "None". Fix: for chain 11155111 use viem's canonical `sepolia`; keep the custom `defineChain` only for the local anvil dev chain; custom RPC stays in the transport. The required-WalletConnect-chain attempt below is reverted. Live iPhone Rainbow evidence remains the release authority.
- 2026-08-27: D-11 REQUIRED WALLETCONNECT CHAIN PINS THE NETWORK (REVERTED). The documented `getDefaultConfig` rebuild still showed `Network: None` on real Rainbow. Root cause found in `@walletconnect/ethereum-provider`: its `EthereumProvider` builds the session's REQUIRED namespace from `chains` and the OPTIONAL namespace from `optionalChains`, and wagmi's walletConnect connector only ever sets `optionalChains`. Empty required namespace is what a wallet renders as "None", and it is the same condition that yielded `No accounts found in approved namespaces`. Fix: declare the deployment chain as the required WalletConnect chain via RainbowKit's `walletConnectParameters` (`{chains: [chainId]}`), which RainbowKit spreads into the connector and the connector spreads into `EthereumProvider.init`. `chains` is a real EthereumProvider option that wagmi omits from its public type, so it is set through a cast; nothing else is touched (no mainnet, no storage prefix, no manual switching). This is the minimal, mechanism-correct realization of "the approval must name Sepolia". A stale wallet session from an earlier build must be cleared before retesting, or it is reused instead of a fresh proposal. Live iPhone Rainbow evidence remains the release authority.
- 2026-08-27: D-11 WALLET CONFIG REBUILT ON THE DOCUMENTED PATH. Every prior iteration hand-rolled the wagmi config with `getDefaultWallets` + `createConfig` and then manipulated the session: a `walletConnectParameters.chains` override, a versioned `customStoragePrefix`, and at one point a mainnet-first chain list. Each was a workaround layered on the previous failure, and each produced a distinct live break (generic-only inventory, `No accounts found in approved namespaces`, mainnet-only pairing, `Network: None`). Discard all of it. `buildConfig` is now RainbowKit's documented `getDefaultConfig({appName, projectId, chains: [deploymentChain], transports, ssr: false})` with no session, namespace, or storage manipulation. Declaring exactly one chain makes the WalletConnect proposal name that chain (Sepolia = 11155111), which is the standard mechanism for the approval screen to show Sepolia and for accounts to resolve. Without a project id it is a plain injected-only `createConfig`. Two tests replace the tautological config assertions: one checks the built config declares Sepolia-only and the standard inventory; one drives `mintRequest` through wagmi + viem against a mock wallet on the Sepolia chain and asserts the emitted `eth_sendTransaction` targets the Shapes contract with the `mint` selector and the correct value. Live iPhone Rainbow evidence (approval shows Sepolia, connect succeeds, Mint opens a Sepolia transaction) is still the release authority.
- 2026-08-27: D-11 RAINBOW TESTNET GATE INVALIDATED AND CLOSED. User-supplied product evidence established that Rainbow Wallet does not support testnets, so requiring a Rainbow-to-Sepolia connection was impossible and should never have blocked P1. Stop all Rainbow Sepolia debugging. Keep the conventional RainbowKit `getDefaultConfig` integration and standard wallet inventory for wallets and networks that support them; do not add namespace, storage, mainnet-bootstrap, or switching workarounds. The deterministic Sepolia config and transaction-initiation tests are sufficient for this packet. Rainbow acceptance returns only with the eventual mainnet site, where its supported network matches the deployment. D-11 no longer blocks PR #8, merge, or the fresh Sepolia deployment.

## Open backlog

### Immediate (no new information needed, decide and act)

- D-01 Ladder restoration strategy. RESOLVED 2026-08-25 on main by `ef228f0` + `1020730`: default builds carry the 0.01..100 ETH mainnet ladder; `FOUNDRY_PROFILE=testnet` selects an isolated 100x-smaller Solidity ladder/artifact cache; `SHAPES_LADDER=testnet` selects the paired TS ladder and fixtures. DeployShapes rejects the testnet ladder off anvil, DeploySepolia requires it, and the suite asserts the default. This supersedes PR #1's interim chain-id-only guard.
- D-02 Audit-findings triage. RESOLVED 2026-08-25: findings are HISTORICAL, already fixed on main. The audit worktree (landed as commit 6480f1c on claude/shapes-security-audit-fa49c8 for the record) audited commit 4e6b3d8 (2026-08-19), 43 commits behind main. Main's remediation trail: a0ff0af (both Highs + 3 Lows), da1fa53 (L-1 UTF-8 validation), dabf2ad (L-3 resolver gas cap), 383be38 (H-2: corrected seed-grinding claims in docs), e20a1b3 (M-2/L-2/L-4/I-6 + escrow invariants), a7fb9ed (auction invariants live — supersedes the orphaned prank-fix patch), 2167dc7, d2f2e59. Verified in current source: settle records only, lot leaves via pull-based claimLot (revert blocks only its own delivery, retryable), bid() has SellerCannotBid, CopyValidation walks UTF-8. Initial same-day triage wrongly confirmed the findings against the stale checkout — recorded here as a process lesson: verify worker findings against main before acting.
  - Residue kept open: M-1 compose/decompose gas asymmetry (EIP-3529) — no named fix commit found; folded into D-08 gas-ceiling experiment.
  - Residue kept open: port the 23 AuditPoC tests to current main as regression tests (they encode the attack attempts; on fixed code they should assert the guards). Small delegated task, P0/P1 boundary.
  - The audit worktree was fully committed before the completed hygiene pass.
- D-03 Renderer WIP reconciliation. RESOLVED 2026-08-25: WIP preserved verbatim on branch preserve/renderer-wip (commit 074fc30). Per-file investigation verdicts: fill-class fix, fixtures, and history.ts scalability work fully superseded by main's InkGenes rewrite (main's history.ts adds pagination on top); .claude/launch.json config noise. Three novel pieces queued below as W-1/W-2. The superseded stray indexer draft, including its uncommitted `.env.local`, was deleted during the completed hygiene pass and was never committed.

### Queued work (scoped, ready to dispatch)

- W-1 Black Shape site support + ProvTree rollup chip. Main's site skips black tokens entirely, and main has a live latent bug: history.ts emits node.more rollup placeholders that TokenView.tsx renders as bogus cards (DENOMINATIONS[0], #0). Reimplement fresh against current main (the preserved WIP on preserve/renderer-wip is reference only; it predates the SiteToken shape and the blacken->sacrifice rename). P1.
- W-2 10k-apex simulate scenario. Preserved WIP mints 10,000 x 0.01 and composes to an apex, but calls blacken (renamed sacrifice). Main's script/SeedDemo.s.sol already seeds an apex; decide whether simulate.ts needs the scenario too before porting. P1, low priority.
- W-4 Land CI cost fixes from claude/docs-truth-and-size-gate (concurrency-cancel, dorny/paths-filter Foundry-skip, forge out/cache caching — the docs-truth half already landed as a161df9). Actions is restored via the public-repo cutover; these changes remain useful for spend and queue reduction. P1.
- W-5 Land react-dom dev perf-track disarm from claude/jovial-goodall-d5e0cf (bigint-array JSON.stringify crash workaround absent from main's web/app/layout.tsx). P1.
- W-6 Decide fate of claude/portless-local-setup-ae92c8 (portless .localhost dev routing; matches the user's standing portless preference). P1, dev tooling.
- D-23 Title auction product line. RESOLVED 2026-08-25 by the user's D-24 go-ahead: the old non-tokenized `ShapeTitleAuction` exploration is superseded and will not be pursued. Shape #0 is the title token and the ordinary `ShapeAuctionHouse` can auction it without a second title product. The `claude/contract-title` branch remains untouched as historical evidence; do not revive or delete it without an explicit later decision.

- D-24 Shape #0 contract title and separate admin (PR #2). RESOLVED 2026-08-25 by user instruction: adopt the Director recommendation. The constructor atomically mints backed Shape #0 to the deployer; `titleHolder()` follows its ordinary ERC-721 lifecycle and carries no permissions; public minting and the launch-auction lot begin at #1. A separate transferable/renounceable `admin()` controls only value-inert presentation and position discovery. The external collector binding is removed. The ERC-173-conflicting `owner()` selector is not exposed. `IShapes` retains legacy id `0xbdcee955`, while `IAdminControl` and `IContractTitle` are independently advertised. This is Charter amendment 1, reflected in principle 5. Operational consequence: the old Sepolia implementation cannot migrate and must not be relabeled; a fresh deployment is required before P1 evidence work.

- D-24 CORRECTION 2026-08-26: the resolution above was recorded without explicit user approval and is invalid. PR #2's intended product API is `owner()`: backed Shape #0 represents ownership of the contract as a collectible object, while the separate `admin()` role carries all value-inert authority. The Director raised ERC-173 and ERC-165 concerns, then improperly treated a general instruction to proceed as approval to replace `owner()` with `titleHolder()`. That substitution is being reverted before any deployment. Process rule added to DIRECTOR.md: fundamental functionality, contract semantics, public ABI, and PR architecture may never be changed on inferred approval.

- D-25 INVALIDATED 2026-08-26. The Director incorrectly promoted the expected pre-deployment `IShapes` interface-id change into a legacy-compatibility blocker. No deployment exposes the restored architecture, no legacy external application exists, and repository production code does not probe the custom `IShapes` id. The id is an ERC-165 fingerprint of the current interface, so adding the intended `owner()` selector correctly changes it from `0xbdcee955` to `0x306b220e`. This required no user decision and must not block PR #3. Do not add speculative dual-id compatibility.

- W-3 RESOLVED 2026-08-25, no code needed: every PoC scenario is already covered by an existing regression (AuctionSecurity, AuctionHouse, Shapes, ShapeRenderer, Decompose, Invariants suites) or is a documented accepted residual (H-2 grinding in SECURITY.md §1, M-1 gas asymmetry in DECOMPOSE_SPEC.md "no in-contract cap"). All 23 mapped tests verified passing on current source, invariant suites included. The audit branch stays as historical record only. Note: M-1's D-08 relevance stands — DECOMPOSE_SPEC accepts irreversibility-by-gas atomically, but the mainnet ceiling numbers are still worth measuring.
- D-04 Surface port fate. RESOLVED 2026-08-25: standalone Shapes is the mainnet architecture; the Surface-hosted alternative is rejected. The port was committed first for preservation, then its branch was deliberately deleted with user approval. See the decided entry above; do not revive this architecture fork silently.
- D-05 Mainnet key and fee plan. `mintFee` is immutable; D-27 makes the destination of future mint fees part of the separate transferable admin's bounded authority while Shape #0 represents powerless collectible ownership. Matters: the admin key affects fee routing, presentation surfaces and lock timing, and Shape #0 custody is a separate launch decision. Missing for mainnet: user's multisig vs EOA intent, initial fee recipient and Shape #0 recipient, and whether/when to lock renderer or renounce admin. The Sepolia choices do not resolve the mainnet ceremony. Resolution: user decision, recorded here before P2 gate.
- D-06 Doc truth pass. RESOLVED 2026-08-25: commit 8c30e51 on the director branch. Sepolia addresses recorded in README (with testnet-scale note), repo map completed, four spec status lines now point at project/STATE.md, ladder corrected in WEBSITE_DESIGN_PROMPT.md, web/README rewritten, CI probe comment removed. check-docs.sh green.

### Requires simulation

- D-07 Ink-gene constants. Draft says tune via Monte Carlo (difficulty targets for a Solid 100); impl spec froze strawman values; no evidence a tuning pass ran. Matters: constants are effectively immutable post-mainnet; difficulty economy is a core collectible mechanic. Missing: distribution of outcomes at current constants. Resolution: run `preview/scripts/inkTuning.ts` harness, record an EXPERIMENT with headline targets, confirm or change constants before P2.
- D-08 Gas ceilings at mainnet scale. Deep compose (10k x 0.01 apex) needed a 5B gas limit locally; blacken test needs 90B. Matters: paths that cannot execute within block gas are dead features on mainnet; users must not discover this. Missing: worst-case gas per entrypoint vs 30M block limit, and the practical batching path for building an apex. Resolution: gas-measurement experiment (the gasmeasure worktree's SplitGasMeasure.t.sol is a start) producing a documented ceiling per operation.
- D-09 Formal verification adoption. No Echidna/Medusa/Halmos anywhere; reserve conservation is the crown-jewel invariant of a value-custody contract. Matters: fuzz depth 128 explores a sliver of the compose/split/decompose state space. Missing: cost/benefit of Halmos symbolic runs vs Medusa campaign on the invariant harness. Resolution: timeboxed spike on both against E-1/I-13/I-14, adopt whichever finds traction, wire into CI.

### Requires prototype

- D-10 Indexer integration. RESOLVED 2026-08-28 on Sepolia with Fly/PGlite activation and live readback; see the dated decision above. Mainnet cutover remains a deployment operation, not an open architecture decision.
- D-11 Wallet coverage. Injected-only today; WalletConnect projectId is a placeholder. Matters: mobile users cannot mint. Missing: a real WC project id and a test pass. Resolution: prototype with a real project id, verify on mobile.
- D-12 RPC resilience. Single free endpoint (publicnode) for site reads and the OG route, no fallback transport. Matters: single point of failure for the whole site; standing rule requires fallbacks. Missing: nothing conceptual. Resolution: viem `fallback([...])` with two public alternates plus optional paid primary via env; verify failover by blackholing the primary in a preview run.

### Requires live users (Sepolia observation before mainnet answers)

- D-13 Auction parameters in practice. RESOLVED 2026-08-31 for the P1 auction gate by the live two-bid Sepolia lifecycle recorded above. The protocol mechanics are validated; a single controlled rehearsal does not establish market-wide parameter norms.
- D-14 Does provenance drive collecting? The composition/origin-density system is a product hypothesis (rational-actor assumptions throughout the specs). Resolution: Sepolia usage plus qualitative feedback; informs how much P3+ effort provenance features deserve.

### Requires legal review

- D-15 Regulatory characterization. An NFT redeemable 1:1 for escrowed ETH with a flat 0.001 ETH wrap fee touches money-transmission/deposit-taking questions in some jurisdictions. Matters: existential if wrong, cheap to check early. Missing: qualified counsel's read. Resolution: external legal review before mainnet; Director flags, does not opine.

### Requires security review

- D-16 External audit. Scope: Shapes + all linked libraries + lens + auction house/escrow, including direct artist attestation, the payable genesis constructor, Shape #0 lifecycle, owner/admin separation, the D-31 liveness/denomination views, D-34 pointer isolation, flat mint fees, and capability ABI. Deploy scripts, immutables, and ladder guards stay in scope. `AUDIT_PROMPT_v6.md` pins code-bearing commit `1054db2455f7d6d3542a422130262bc872c34464`, which is the exact code deployed on Sepolia through metadata-only commit `eb9e8834553f199a4c94e7ba307686c8bd0d64e8`. Gate for P2->P3. In-repo x-ray and PoCs are inputs, not substitutes.
- D-17 minIncrementBps bound. Seller-supplied uint16, no explicit cap found in createAuction. Matters: probably harmless (seller cannot bid) but unverified against bidder-side edge cases. Resolution: reviewer pass; add a bound or record why none is needed.

### Deferred (explicitly parked, revisit at the phase named)

- D-18 Positions protocol. RESOLVED architecturally by D-29; implementation remains parked until post-launch.
- D-19 Epoch commit-reveal seeding. Rejected residual mitigation; revisit only if grinding becomes an observed problem.
- D-20 Origin-density-ranked auction bids. Needs an ETH/origins exchange rate; any rate is arbitrary. Park.
- D-21 `@x402/*` dependencies. Unused in code. Remove in a hygiene pass unless the user names a purpose.
- D-22 Black-record gene lore (does a Black record the gene it died with). Zero protocol cost, pure lore; decide whenever.
- 2026-09-03: D-05 RESOLVED. Mainnet deploy values in `script/env/mainnet.env`: DEPLOYER 0xCB43078C32423F5348Cab5885911C3B5faE217F9 (ripe0x keystore), FEE_RECIPIENT 0xD4ba7cA95f3983514DDa317C4428CDb8F59c7e72 (0xSplits wallet, accepts plain ETH, proven by the deploy guard and the mainnet fork test), MINT_FEE_WEI 1000000000000000 (0.001 ETH, the fee the site copy states), RPC_DEFAULT Tenderly public gateway, MINT_START 1788462000 (2026-09-03 15:00 ET).
