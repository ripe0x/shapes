out/
cache/
broadcast/
node_modules/
preview/dist/
preview/shapes-preview.html
docs/
.env

# local scratch, not part of the project
_to_delete/
shapes-preview.html
shapes-demo.gif
*.tar.gz
preview/public/ink-demo.html

# Testnet-profile build artifacts (foundry.toml [profile.testnet]).
out-testnet/
cache-testnet/

# Local Netlify folder
.netlify

# Local anvil deploy record (deployments/<chainId>.json). Sepolia and mainnet records are
# committed by the cutover PR.
deployments/31337.json

# ephemeral root config written by script/medusa.sh
/medusa.json

# macOS Finder metadata
.DS_Store
